<p>
  <%= form.collection_select(:trello_board_id, Trello::Board.all, :id, :name, prompt: true) %>
  Target Trello board.
</p>

<p>
  <%= form.check_box :trello_board_sync %>
  Enable Redmine to Trello Board synchronization?
</p>

<p>
  <%= form.text_area :trello_mapping_redmine_statuses %><br />
  Redmine statuses mapping, enter one status per line.
</p>

<p>
  <%= form.text_area :trello_mapping_trello_lists %><br />
  Trello lists mapping, enter one list per line.
</p>

<p>
  <%= form.collection_select(:trello_excluded_trackers, @project.trackers, :id, :name,
  {selected: JSON.parse(@project.trello_excluded_trackers)},
  {multiple: true}) %><br />
  Select excluded tracker(s) from synchronization.
</p>